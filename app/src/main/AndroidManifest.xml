<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalLucasEzequielIvanYLeo"
        tools:targetApi="31">

        <!-- Declaración de la actividad ActivityQuestion -->
        <activity
            android:name=".ActivityQuestion"
            android:exported="true">
        </activity>

        <!-- Declaración de la actividad RestQuestion como actividad principal -->
        <activity
            android:name=".RestQuestion"
            android:exported="true">
            <intent-filter>
                <!-- Definir como la actividad principal que se lanza al abrir la aplicación -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
